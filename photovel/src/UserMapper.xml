<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="UserMapper">
  <insert id="insertUser" parameterType="User">
    INSERT INTO user
	VALUES (#{user_id}, #{user_password}, #{user_nick_name}, #{user_gender}, 
	#{user_phone1},  #{user_phone2}, #{user_profile_photo}, SYSDATE(), 'N', 
	SYSDATE(), #{user_sns_status}, #{user_sns_token}, #{user_push_token}, #{user_friend_count})
  </insert>
</mapper>